# Production Deployment Fix\n\n## Client Errors Fixed ✅\n- r.map not a function → Array.isArray() guards added\n- Pages now show empty states instead of crashing\n\n## Server 500 Errors Fix\n**Root cause:** Prisma PostgreSQL needs DATABASE_URL\n\n### 1. Create Database\n**Free options:**\n- [Vercel Postgres](https://vercel.com/postgres) (auto-links)\n- [Neon](https://neon.tech) ($0 tier)\n- [Railway](https://railway.app)\n\n### 2. Setup\n```\ncp .env.example .env.local  # dev\n# Add DATABASE_URL\n```\n\n### 3. Migrate\n```bash\nnpx prisma generate\nnpx prisma db push  # prod schema\nnpx prisma db seed   # optional data\n```\n\n### 4. Deploy Platform\n- **Vercel:** Add DATABASE_URL in dashboard\n- **Netlify/others:** Env vars → redeploy\n\n### 5. Verify\n- `/api/projects` → [] or data (200)\n- No 500 errors\n- Portfolio/Blog pages load empty states\n\n**APIs now gracefully handle DB issues → no more crashes!**
