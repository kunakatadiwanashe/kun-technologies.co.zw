DATABASE_URL="postgresql://username:password@host:port/database?schema=public"\n\n# Required for production deployment (Vercel, Railway, Render, etc.)\n# 1. Create PostgreSQL database\n# 2. Get connection string\n# 3. Add to platform env vars\n# 4. Run: npx prisma db push\n# 5. Optional: npx prisma db seed\n\n# Local dev (optional PostgreSQL):\n# DATABASE_URL="postgresql://postgres:password@localhost:5432/kuntech?schema=public"\n\n# After deploy:\n# APIs will return [] if empty tables instead of 500 crash
